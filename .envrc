use asdf

# update go variables with project-local values
export GOPATH=$(asdf where golang)/packages
export GOROOT=$(asdf where golang)/go
export PATH="$(go env GOPATH)/bin:${PATH}" # add gobin to path
